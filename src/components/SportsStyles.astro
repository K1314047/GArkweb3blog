<style is:inline>
  /* Strava Card - 极简 Modern 风格 */
  .strava-card {
    position: relative;
    /* 优化背景：使用细腻的渐变代替单一灰色，并加入微弱的橙色光晕呼应品牌色 */
    background: #ffffff;
    background-image: radial-gradient(circle at 90% 10%, rgba(252, 76, 2, 0.03) 0%, transparent 40%),
                      linear-gradient(180deg, #ffffff 0%, #f7f7f7 100%);
    border-radius: 24px;
    overflow: hidden;
    margin-bottom: 3rem;
    width: 100%;
    /* 极淡的边框，几乎不可见，主要靠光影区分 */
    border: 1px solid rgba(0,0,0,0.02);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1), 
                box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1),
                border-color 0.3s ease;
  }

  .strava-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(0,0,0,0.06);
    border-color: rgba(252, 76, 2, 0.08); /* 悬浮时边框极其微弱地泛橙色 */
  }

  :global([data-theme="dark"]) .strava-card {
    background: #1a1a1a;
    background-image: radial-gradient(circle at 90% 10%, rgba(252, 76, 2, 0.08) 0%, transparent 40%),
                      linear-gradient(180deg, #222222 0%, #1a1a1a 100%);
    border-color: rgba(255,255,255,0.03);
  }

  :global([data-theme="dark"]) .strava-card:hover {
    box-shadow: 0 12px 32px rgba(0,0,0,0.2);
    border-color: rgba(252, 76, 2, 0.15);
  }

  .strava-header {
    position: absolute;
    top: 1.25rem;
    left: 1.5rem;
    z-index: 2;
    display: flex;
    flex-direction: column;
    pointer-events: none; /* Let clicks pass through */
  }

  .strava-year {
    font-size: 1.5rem;
    font-weight: 800;
    line-height: 1;
    color: var(--heading-color);
    letter-spacing: -0.03em;
    font-family: var(--font-heading, inherit);
  }

  .strava-subtitle {
    font-size: 0.6rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--gray-color);
    font-weight: 600;
    margin-top: 0.25rem;
    opacity: 0.8;
  }

  .poster-wrap {
    padding: 1rem;
    padding-top: 4rem; /* Adjusted padding for smaller header */
    display: flex;
    justify-content: center;
    align-items: center;
    background: linear-gradient(to bottom, rgba(0,0,0,0.01), transparent);
    min-height: 200px;
  }

  .poster-img {
    display: block;
    width: 100%;
    height: auto;
    max-height: 260px;
    object-fit: contain;
    mix-blend-mode: multiply;
    /* Removed hover effects: always keep original look */
    /* filter: grayscale(100%) opacity(0.8); */
    /* transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1); */
    transform-origin: center;
    opacity: 0.9;
  }

  /* Removed hover effect on poster image */
  /* .strava-card:hover .poster-img {
    filter: grayscale(0%) opacity(1);
    transform: scale(1.03);
  } */
  
  :global([data-theme="dark"]) .poster-img {
    mix-blend-mode: screen;
    opacity: 0.8;
    /* filter: grayscale(100%) opacity(0.7) contrast(1.2); */
  }
  
  /* Removed hover effect on dark mode poster image */
  /* :global([data-theme="dark"]) .strava-card:hover .poster-img {
    filter: grayscale(0%) opacity(0.9) contrast(1.1);
  } */

  .strava-btn {
    position: absolute;
    top: 1.25rem;
    right: 1.5rem;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--background-color);
    border-radius: 50%;
    color: #FC4C02 !important;
    box-shadow: 0 2px 8px rgba(0,0,0,0.06);
    transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
    z-index: 2;
    border: 1px solid rgba(0,0,0,0.05);
    opacity: 0.9;
  }

  .strava-btn:hover { 
    transform: scale(1.1) rotate(8deg);
    box-shadow: 0 4px 12px rgba(252, 76, 2, 0.25);
    background: #FC4C02;
    color: white !important;
    border-color: transparent;
    opacity: 1;
  }

  .strava-btn svg {
    width: 14px;
    height: 14px;
  }

  /* Empty State */
  .empty-state {
    padding: 3rem 2rem;
    text-align: center;
    color: var(--gray-color);
    background: var(--code-background-color);
    border-radius: 12px;
    border: 1px dashed rgba(0,0,0,0.08);
  }

  /* Table */
  .tabs-content table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    margin: 1.5rem 0;
    font-size: 0.9rem;
  }
  
  .tabs-content th {
    text-align: left;
    padding: 1rem;
    border-bottom: 1px solid rgba(0,0,0,0.06);
    color: var(--gray-color);
    font-weight: 600;
    font-size: 0.8em;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .tabs-content td {
    padding: 1rem;
    border-bottom: 1px solid rgba(0,0,0,0.03);
    color: var(--text-color);
    transition: background-color 0.2s;
  }
  
  .tabs-content tr:last-child td { border-bottom: none; }
  .tabs-content tbody tr:hover td { 
    background-color: var(--code-background-color); 
    color: var(--heading-color);
  }
  .tabs-content tbody tr td:first-child {
    border-top-left-radius: 8px;
    border-bottom-left-radius: 8px;
  }
  .tabs-content tbody tr td:last-child {
    border-top-right-radius: 8px;
    border-bottom-right-radius: 8px;
  }
</style>
